# V2 迁移前代码归档

本目录保存 Vue 迁移前的原生 HTML、CSS 和 JavaScript 文件，以及旧版页面直接引用的图片资源。

当前维护和启动入口仍是上一级目录的 Vue 3 项目：

```text
../index.html
../src/
../package.json
```

`code_v1` 仅用于查看、对比和恢复迁移前实现，不参与当前 Vite 构建，也不应直接与 `../src` 混合修改。
